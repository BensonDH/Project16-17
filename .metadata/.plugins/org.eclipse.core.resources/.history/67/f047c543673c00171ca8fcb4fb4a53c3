package asteroids.programs.expressions;

import asteroids.model.Entity;
import asteroids.part3.programs.SourceLocation;
import asteroids.programs.Program;

public class EntityLiteralExpression<T extends Entity> extends Expression implements Literal {

	public EntityLiteralExpression(SourceLocation sourceLocation, T entity) {
		super(sourceLocation);
		this.entity = entity;
	}
	
	public EntityLiteralExpression(T entity) {
		super(null);
		this.entity = entity;
	}

	@Override
	public T getValue() {
		return this.entity;
	}

	@Override
	public Literal eval(Program parentProgram) {
		return this;
	}

	/**
	 * Variable registering this EntityLiteral's value.
	 */
	private final T entity;
}
