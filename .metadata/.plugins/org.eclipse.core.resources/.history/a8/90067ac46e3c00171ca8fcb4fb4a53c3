package asteroids.tests;

import static org.junit.Assert.*;

import java.util.*;
import java.util.stream.Collectors;

import org.junit.*;
import asteroids.model.*;
import asteroids.programs.expressions.*;

public class RandomTests {
	@Test
	public void test(){
	Boolean testbool = true;
	Double testdouble = Double.valueOf(2);
	
	Ship entity = new Ship(0,0,0,0, 20, 0);
	
	EntityLiteralExpression<Ship> shipLiteral = new EntityLiteralExpression<Ship>(entity);
		
	Variable<EntityLiteralExpression> testVar = new Variable<>(EntityLiteralExpression.class, "test", shipLiteral);
	
	System.out.println(testVar);
	
	}
}