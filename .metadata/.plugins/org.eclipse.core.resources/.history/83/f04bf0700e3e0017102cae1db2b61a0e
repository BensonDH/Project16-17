package asteroids.tests;

import static org.junit.Assert.*;

import java.util.*;
import java.util.stream.Collectors;

import org.junit.*;
import asteroids.model.*;
import asteroids.programs.expressions.*;

public class RandomTests {
	@Test
	public void test(){
		
	Expression<Double> testexp = new ChangeSignExpression(new Literal<Double>(Double.class, Double.valueOf(5)));
	
	System.out.println(testexp instanceof SupportedType);
	
	}
}